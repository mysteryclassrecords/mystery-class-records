.merch-block-names-container {
    flex: 3;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: left;
    padding: 1vw;
    border-right: clamp(3px, 0.5vw + 3px, 4px) solid #0B1215;
    text-align: left;
    height: 100%; /* Ensures it fills the assigned space */
    overflow: hidden; /* Prevents text from overflowing */
  }
  
  .merch-block-artist-name,
  .merch-block-product-name {
    white-space: nowrap; /* Prevents wrapping */
    overflow: hidden; /* Hides any overflow text */
    text-overflow: ellipsis; /* Adds "..." to overflowing text */
    max-height: 1.2em; /* Adjust based on font size to prevent excessive growth */
    line-height: 1.2; /* Ensures consistent spacing */
  }
  